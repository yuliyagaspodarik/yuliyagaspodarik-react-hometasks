import React from 'react';

import './Counter.css';
/*
export function Counter(props) {
  return (
    <React.Fragment>
      <span id={props.textId}>{props.optionCounter}: </span>
      <span id={props.totalId}>{props.count}</span>
    </React.Fragment>
  )
}
*/
